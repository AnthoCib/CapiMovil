@model (string Titulo, string Mensaje, string Icono)

<div class="pf-empty">
    <i class="bi @Model.Icono"></i>
    <p class="fw-semibold mb-1">@Model.Titulo</p>
    <p class="mb-0">@Model.Mensaje</p>
</div>
