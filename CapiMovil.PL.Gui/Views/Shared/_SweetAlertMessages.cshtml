@using System.Text.Json

@if (TempData["ok"] != null)
{
    <script>
        Swal.fire({ icon: 'success', title: 'Operación completada', text: @Html.Raw(JsonSerializer.Serialize(TempData["ok"]?.ToString())), confirmButtonText: 'Aceptar' });
    </script>
}

@if (TempData["error"] != null)
{
    <script>
        Swal.fire({ icon: 'error', title: 'No se pudo completar', text: @Html.Raw(JsonSerializer.Serialize(TempData["error"]?.ToString())), confirmButtonText: 'Aceptar' });
    </script>
}

@if (ViewBag.SwalError != null)
{
    <script>
        Swal.fire({ icon: 'error', title: 'No se pudo completar', text: @Html.Raw(JsonSerializer.Serialize(ViewBag.SwalError?.ToString())), confirmButtonText: 'Aceptar' });
    </script>
}
