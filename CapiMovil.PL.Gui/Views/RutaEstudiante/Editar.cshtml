@model CapiMovil.PL.Gui.Models.ViewModels.RutaEstudianteFormViewModel

@{
    ViewData["Title"] = "Editar Asignación";
}

<div class="row justify-content-center">
    <div class="col-lg-10">
        <div class="card shadow-sm border-0">
            <div class="card-header bg-white border-0 pt-4 pb-0">
                <h2 class="h4 fw-bold mb-1"><i class="bi bi-pencil-square text-primary me-2"></i>Editar Asignación Ruta - Estudiante</h2>
                <p class="text-muted mb-0">El estado de asignación se deriva del switch general de estado.</p>
            </div>
            <div class="card-body">
                <form asp-action="Editar" method="post">
                    @Html.AntiForgeryToken()
                    <input asp-for="IdRutaEstudiante" type="hidden" />

                    <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

                    <div class="row g-3">
                        <div class="col-md-4">
                            <label asp-for="CodigoRutaEstudiante" class="form-label"></label>
                            <input asp-for="CodigoRutaEstudiante" class="form-control bg-light" readonly />
                        </div>
                        <div class="col-md-4">
                            <label asp-for="FechaInicioVigencia" class="form-label"></label>
                            <input asp-for="FechaInicioVigencia" class="form-control" />
                            <span asp-validation-for="FechaInicioVigencia" class="text-danger"></span>
                        </div>
                        <div class="col-md-4">
                            <label asp-for="FechaFinVigencia" class="form-label"></label>
                            <input asp-for="FechaFinVigencia" class="form-control" />
                        </div>

                        <div class="col-md-6">
                            <label asp-for="IdRuta" class="form-label"></label>
                            <select asp-for="IdRuta" asp-items="Model.Rutas" class="form-select">
                                <option value="">Seleccione una ruta</option>
                            </select>
                            <span asp-validation-for="IdRuta" class="text-danger"></span>
                        </div>
                        <div class="col-md-6">
                            <label asp-for="IdEstudiante" class="form-label"></label>
                            <select asp-for="IdEstudiante" asp-items="Model.Estudiantes" class="form-select">
                                <option value="">Seleccione un estudiante</option>
                            </select>
                            <span asp-validation-for="IdEstudiante" class="text-danger"></span>
                        </div>

                        <div class="col-md-6">
                            <label asp-for="IdParaderoSubida" class="form-label"></label>
                            <select asp-for="IdParaderoSubida" asp-items="Model.ParaderosSubida" class="form-select">
                                <option value="">Seleccione un paradero</option>
                            </select>
                        </div>

                        <div class="col-md-6">
                            <label asp-for="IdParaderoBajada" class="form-label"></label>
                            <select asp-for="IdParaderoBajada" asp-items="Model.ParaderosBajada" class="form-select">
                                <option value="">Seleccione un paradero</option>
                            </select>
                        </div>
                        <div class="col-12">
                            <label class="form-label">Dirección de Paradero de Bajada (Punto final de ruta)</label>
                            <input id="txtDireccionFinRuta" class="form-control" readonly />
                        </div>

                        <div class="col-12">
                            <label asp-for="Observaciones" class="form-label"></label>
                            <textarea asp-for="Observaciones" class="form-control" rows="2"></textarea>
                        </div>

                        <div class="col-12">
                            <div class="form-check form-switch mb-0">
                                <input asp-for="Estado" class="form-check-input" />
                                <label asp-for="Estado" class="form-check-label"></label>
                            </div>
                            <small class="text-muted">Si desactivas este switch, el estado de asignación se guardará como INACTIVO.</small>
                        </div>
                    </div>

                    <input asp-for="EstadoAsignacion" type="hidden" value="@Model.EstadoAsignacion" />

                    <div class="d-flex gap-2 mt-4">
                        <button type="submit" class="btn btn-primary"><i class="bi bi-save me-1"></i>Actualizar</button>
                        <a asp-action="Listar" class="btn btn-outline-secondary"><i class="bi bi-arrow-left me-1"></i>Cancelar</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            const rutaSelect = document.getElementById('IdRuta');
            const subidaSelect = document.getElementById('IdParaderoSubida');
            const bajadaSelect = document.getElementById('IdParaderoBajada');
            const direccionFinInput = document.getElementById('txtDireccionFinRuta');
            const subidaActual = '@Model.IdParaderoSubida';
            const bajadaActual = '@Model.IdParaderoBajada';

            async function cargarParaderos() {
                const idRuta = rutaSelect.value;
                subidaSelect.innerHTML = '<option value="">Seleccione un paradero</option>';
                bajadaSelect.innerHTML = '<option value="">Seleccione un paradero</option>';
                direccionFinInput.value = '';
                if (!idRuta) return;

                const response = await fetch(`/RutaEstudiante/ObtenerParaderosPorRuta?idRuta=${idRuta}`);
                const payload = await response.json();
                if (!payload.ok) return;

                payload.data.forEach(function (item) {
                    const selectedSubida = item.value === subidaActual ? 'selected' : '';
                    const selectedBajada = item.value === bajadaActual ? 'selected' : '';
                    subidaSelect.insertAdjacentHTML('beforeend', `<option value="${item.value}" ${selectedSubida}>${item.text}</option>`);
                    bajadaSelect.insertAdjacentHTML('beforeend', `<option value="${item.value}" ${selectedBajada}>${item.text}</option>`);
                });

                direccionFinInput.value = payload.direccionFin || 'No disponible';
            }

            rutaSelect.addEventListener('change', cargarParaderos);
            if (rutaSelect.value) cargarParaderos();
        });
    </script>
}
